names = ['bob', 'joe', 'susan', 'margaret']
# names['margaret'] = 'jody'

# We get an error running this code because we use a
# string ("margaret") as an index to change an element in the array into another one.

names[3] = 'jody'

p names